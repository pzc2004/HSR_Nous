# stage.yaml — 关卡配置（运行时输入）
# 示例：忘却之庭 第12层 上半

stage:
  stage_template: "FH_12_1_upper"    # 引用 data/sim_templates/stages/FH_12_1_upper.yaml

  # 运行时覆盖：敌人等级
  enemy_level_overrides:
    "1002011": 95

  # 环境修正：额外 modifier（可选）
  environment_overrides:
    modifiers: []
